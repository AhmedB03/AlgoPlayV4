export type TestCase = { args: unknown[]; expected: unknown; label: string };
export type Problem = { id: string; number: number; title: string; category: string; difficulty: string; summary: string; detail: string; example: string; output: string; explanation: string; constraints: string[]; hint: string; analogy: string; complexity: [string,string]; functionName: string; starter: string; solution: string; tests: TestCase[]; demo: string; pseudocode: string[] };
export const problems: Problem[] = [
{ id:'two-sum', number:1, title:'Two Sum', category:'Arrays & Hashing', difficulty:'Easy', summary:'Find the pair that adds up.', detail:'Given an array of integers nums and an integer target, return the indices of two different numbers whose sum equals target. You may return the indices in any order. Each input has exactly one valid pair.', example:'nums = [2, 7, 11, 15], target = 9', output:'[0, 1]', explanation:'The numbers at indices 0 and 1 are 2 and 7. Together, they make 9.', constraints:['2 ≤ nums.length ≤ 10,000','−10⁹ ≤ nums[i], target ≤ 10⁹','Use each array element at most once.'],hint:'For each number, look for target − number in a map of numbers you have already seen.',analogy:'Imagine finding two building blocks that together reach a height of 9. Remember each block you pass so you can find its partner.',complexity:['O(n)','O(n)'],functionName:'twoSum',starter:'function twoSum(nums, target) {\n  // Find two indices that add up to target.\n  \n}',solution:'function twoSum(nums, target) {\n  const seen = new Map();\n  for (let i = 0; i < nums.length; i++) {\n    const need = target - nums[i];\n    if (seen.has(need)) return [seen.get(need), i];\n    seen.set(nums[i], i);\n  }\n  return [];\n}',tests:[{args:[[2,7,11,15],9],expected:[0,1],label:'A simple pair'},{args:[[3,2,4],6],expected:[1,2],label:'Pair in the middle'},{args:[[3,3],6],expected:[0,1],label:'Duplicate values'},{args:[[-3,4,3,90],0],expected:[0,2],label:'Negative numbers'},{args:[[0,4,3,0],0],expected:[0,3],label:'Two zeroes'}],demo:'{"nums":[2,7,11,15],"target":9}',pseudocode:['Create an empty memory map','Visit the next number','Find the missing partner','Look for that partner in memory','Remember this number and its index','Return the two matching indices']},
{ id:'reverse-list',number:2,title:'Reverse Linked List',category:'Linked Lists',difficulty:'Easy',summary:'Turn every connection around.',detail:'Given the head of a singly linked list, reverse its links and return the new head. Each node has a val and a next property. The runner builds the linked list from the input array and converts your returned list back to an array.',example:'head = [1, 2, 3, 4, 5]',output:'[5, 4, 3, 2, 1]',explanation:'The last node becomes the new head. Every next pointer faces the opposite direction.',constraints:['0 ≤ number of nodes ≤ 5,000','−5,000 ≤ node.val ≤ 5,000','Return the new head, not an array.'],hint:'Keep three pointers: previous, current, and the saved next node. Save next before changing a link.',analogy:'A line of friends is holding hands in one direction. Turn each person around, one at a time, without losing the rest of the line.',complexity:['O(n)','O(1)'],functionName:'reverseList',starter:'function reverseList(head) {\n  // Nodes have .val and .next properties.\n  \n}',solution:'function reverseList(head) {\n  let prev = null;\n  let curr = head;\n  while (curr) {\n    const next = curr.next;\n    curr.next = prev;\n    prev = curr;\n    curr = next;\n  }\n  return prev;\n}',tests:[{args:[[1,2,3,4,5]],expected:[5,4,3,2,1],label:'Five nodes'},{args:[[1,2]],expected:[2,1],label:'Two nodes'},{args:[[]],expected:[],label:'Empty list'},{args:[[7]],expected:[7],label:'One node'},{args:[[1,1,2]],expected:[2,1,1],label:'Repeated values'}],demo:'{"values":[1,2,3,4,5]}',pseudocode:['Start previous at null, current at head','Save the next node','Turn current.next toward previous','Move previous to current','Move current to the saved next node','Return previous as the new head']},
{ id:'palindrome',number:3,title:'Valid Palindrome',category:'Strings & Two Pointers',difficulty:'Easy',summary:'Meet in the middle.',detail:'Return true if a string reads the same forward and backward after lowercasing it and removing all non-alphanumeric characters. For this exercise, alphanumeric means the English letters a–z and digits 0–9.',example:'s = "A man, a plan, a canal: Panama"',output:'true',explanation:'After cleaning, "amanaplanacanalpanama" reads the same in both directions.',constraints:['0 ≤ s.length ≤ 200,000','s contains printable characters.','An empty cleaned string is a palindrome.'],hint:'Place a pointer at each end. Compare the characters, then move both pointers toward the center.',analogy:'Two readers start at opposite ends of a word. If they always say the same letter, the word is a mirror.',complexity:['O(n)','O(n)'],functionName:'isPalindrome',starter:'function isPalindrome(s) {\n  // Ignore punctuation, spaces, and case.\n  \n}',solution:'function isPalindrome(s) {\n  const text = s.toLowerCase().replace(/[^a-z0-9]/g, "");\n  let left = 0, right = text.length - 1;\n  while (left < right) {\n    if (text[left] !== text[right]) return false;\n    left++;\n    right--;\n  }\n  return true;\n}',tests:[{args:['A man, a plan, a canal: Panama'],expected:true,label:'Ignore punctuation'},{args:['race a car'],expected:false,label:'Not a palindrome'},{args:[' '],expected:true,label:'Empty after cleaning'},{args:['0P'],expected:false,label:'Letters and digits'},{args:['racecar'],expected:true,label:'Mirror word'}],demo:'{"text":"racecar"}',pseudocode:['Clean the string and ignore case','Place a pointer at each end','Compare the two letters','Different letters? Return false','Move both pointers inward','All pairs match: return true']},
{ id:'binary-search',number:4,title:'Binary Search',category:'Binary Search',difficulty:'Easy',summary:'Cut the search in half.',detail:'Given an array nums sorted in ascending order and a target, return the target’s index. If the target is not present, return −1. Your algorithm should run in O(log n) time.',example:'nums = [-1, 0, 3, 5, 9, 12], target = 9',output:'4',explanation:'The value 9 is at index 4. Check the middle and discard the half that cannot contain the target.',constraints:['0 ≤ nums.length ≤ 10,000','Values are unique and sorted ascending.','Return −1 if the target is missing.'],hint:'Maintain inclusive left and right bounds. Compare the target with the middle value to decide which half remains.',analogy:'Finding a page in a book is faster when you open the middle, then choose the half where your page must be.',complexity:['O(log n)','O(1)'],functionName:'search',starter:'function search(nums, target) {\n  // Return the target index, or -1.\n  \n}',solution:'function search(nums, target) {\n  let left = 0, right = nums.length - 1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] === target) return mid;\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return -1;\n}',tests:[{args:[[-1,0,3,5,9,12],9],expected:4,label:'Target exists'},{args:[[-1,0,3,5,9,12],2],expected:-1,label:'Missing target'},{args:[[5],5],expected:0,label:'Single value'},{args:[[],3],expected:-1,label:'Empty array'},{args:[[1,2,3,4,5],1],expected:0,label:'First value'}],demo:'{"nums":[-1,0,3,5,9,12],"target":9}',pseudocode:['Set left and right search bounds','Find the middle index','Compare the middle with target','Keep only the right half','Keep only the left half','Return the index, or −1 if empty']},
{ id:'parentheses',number:5,title:'Valid Parentheses',category:'Stacks',difficulty:'Easy',summary:'Match every opening with a closing.',detail:'Given a string containing only parentheses (), square brackets [], and braces {}, determine whether every opening bracket has a matching closing bracket in the correct order.',example:'s = "({[]})"',output:'true',explanation:'The most recently opened bracket must close first. Here, ] closes [, } closes {, and ) closes (. ',constraints:['0 ≤ s.length ≤ 10,000','Only the characters ( ) [ ] { } are allowed.','An empty string is valid.'],hint:'Push opening brackets onto a stack. Each closing bracket should match the one at the top.',analogy:'Think of a stack of plates. The last plate you put on must be the first one you take off.',complexity:['O(n)','O(n)'],functionName:'isValid',starter:'function isValid(s) {\n  // Use a stack to match brackets.\n  \n}',solution:'function isValid(s) {\n  const stack = [];\n  const pairs = { ")": "(", "]": "[", "}": "{" };\n  for (const char of s) {\n    if ("([{ ".trim().includes(char)) stack.push(char);\n    else if (stack.pop() !== pairs[char]) return false;\n  }\n  return stack.length === 0;\n}',tests:[{args:['({[]})'],expected:true,label:'Nested pairs'},{args:['()[]{}'],expected:true,label:'Adjacent pairs'},{args:['(]'],expected:false,label:'Wrong pair'},{args:['([)]'],expected:false,label:'Wrong order'},{args:['(('],expected:false,label:'Unclosed brackets'},{args:[''],expected:true,label:'Empty string'}],demo:'{"text":"({[]})"}',pseudocode:['Start with an empty stack','Read the next bracket','Opening bracket? Push it on top','Closing bracket? Pop and compare','Wrong match? Return false','Return whether the stack is empty']},
{ id:'graph-bfs',number:6,title:'Graph Breadth-First Search',category:'Graphs',difficulty:'Easy',summary:'Explore one neighborhood at a time.',detail:'Given an adjacency list graph and a starting node, return all reachable nodes in breadth-first order. Visit neighbors in the order they appear in each adjacency list. Mark nodes as seen when adding them to the queue, so each node appears once.',example:'graph = {A:[B,C], B:[D,E], C:[F], D:[], E:[], F:[]}, start = A',output:'["A", "B", "C", "D", "E", "F"]',explanation:'Visit A, then its neighbors B and C, then their unvisited neighbors D, E, and F.',constraints:['1 ≤ number of nodes ≤ 1,000','Node labels are strings.','The starting node is in the graph.','The graph may contain cycles or disconnected nodes.'],hint:'A queue is first in, first out. Add unvisited neighbors to the back and take the next node from the front.',analogy:'You are delivering invitations. Visit your home first, then the closest friends, then their friends. Never deliver the same invitation twice.',complexity:['O(V + E)','O(V)'],functionName:'bfs',starter:'function bfs(graph, start) {\n  // Return reachable nodes in BFS order.\n  \n}',solution:'function bfs(graph, start) {\n  const queue = [start], seen = new Set([start]);\n  const order = [];\n  for (let i = 0; i < queue.length; i++) {\n    const node = queue[i];\n    order.push(node);\n    for (const next of graph[node] || []) {\n      if (!seen.has(next)) {\n        seen.add(next);\n        queue.push(next);\n      }\n    }\n  }\n  return order;\n}',tests:[{args:[{A:['B','C'],B:['D','E'],C:['F'],D:[],E:[],F:[]},'A'],expected:['A','B','C','D','E','F'],label:'Branching graph'},{args:[{A:['B'],B:['A','C'],C:['B']},'A'],expected:['A','B','C'],label:'Cycle'},{args:[{A:[],B:[]},'A'],expected:['A'],label:'Disconnected nodes'},{args:[{X:['X']},'X'],expected:['X'],label:'Self-loop'}],demo:'{"graph":{"A":["B","C"],"B":["D","E"],"C":["F"],"D":[],"E":[],"F":[]},"start":"A"}',pseudocode:['Add start to the queue and mark it seen','Take the next node from the queue','Add this node to the visit order','Look at its neighbors','Queue only neighbors not already seen','Repeat until the queue is empty']}
];
